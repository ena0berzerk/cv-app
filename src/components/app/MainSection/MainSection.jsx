import React from "react";

import classes from "./MainSection.module.scss";

export default function Main() {
  return (
    <>
      <main className={classes.root}>
        <section>CV Preview</section>
      </main>
    </>
  );
}
